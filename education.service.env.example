# Скопируйте этот файл в education.service.env и при необходимости измените значения.
# Используется docker-compose-dev.yml (env_file) для контейнера education-content-service.

# URL сервера Seq для отправки логов (переопределяет Serilog:WriteTo:Seq:serverUrl)
Serilog__WriteTo__1__Args__serverUrl=http://seq:5341
